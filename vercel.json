{
  "buildCommand": "cd web_app && npm install && npm run build",
  "outputDirectory": "web_app/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
